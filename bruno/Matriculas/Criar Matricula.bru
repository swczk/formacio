meta {
  name: Criar Matricula
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/matriculas
  body: json
  auth: none
}

body:json {
  {
    "nomeInteressado": "Ana Silva",
    "cursoDesejado": "Digitação",
    "contato": "ana@email.com"
  }
}

script:post-response {
  bru.setVar("matriculaId", res.body.id);
}
